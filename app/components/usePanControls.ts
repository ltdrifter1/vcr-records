'use client';

/**
 * @deprecated Use `useInteractionManager` from `@/lib/navigation`.
 * Kept as a thin re-export so existing imports keep working.
 */
export { useInteractionManager as usePanControls } from '@/lib/navigation';
